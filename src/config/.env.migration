# 数据库配置模板，根据 data-source.ts 生成
DB_TYPE=postgres
DB_HOST=localhost
DB_PORT=3306
DB_USERNAME=root
DB_PASSWORD=123456
DB_DATABASE=water_moon_select_photo_system

# 是否启用同步（生产环境建议为 false）
DB_SYNCHRONIZE=false

# 是否启用日志
DB_LOGGING=false
